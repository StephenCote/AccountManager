this.DoOperation = function(){
	/// TODO: The template contents should be provided by teh template service, not
	/// hard coded into the operation
	///
	window.uwm.createContent("oMain",g_application_path + "Forms/Register.xml");
}